# HISTORY

## 2026-09-24 パスワード算出式の特定

- Linux マシンで `hactoolnet -t pfs0` により v2.2.0 NSP を展開 (NCA 5 個)。Program NCA
  `ea5f8f0b...nca` (3.8GB) から `--exefsdir` で ExeFS (main 12.5MB / sdk 5.3MB / rtld / main.npdm) を抽出。
  アップデート NCA 単体で ExeFS は完全に入っていた (base NCA 不要)。
  鍵は `~/.switch/{prod,title}.keys` にコピーして使用。hactool 本体は不要だった。
- 解析用 PC には ExeFS だけ転送。3DS 版の鍵文字列 (`SjqjE90z8w` 等) は main には無かった。
- NSO を LZ4 展開 (`work/nso2bin.py`)。PLT スタブを走査して import 名を対応付けたところ
  `nn::crypto::GenerateHmacSha256Mac` と `nn::crypto::DecryptAes128Ctr` が import されており、
  BL で呼ばれている場所は `0x7245a8`, `0x724640` (HMAC) と `0x724898` (AES) の 3 か所だけ。
- Ghidra 12.1.3 に ELF 化した main を入れて全自動解析を始めたが、12MB の text で時間がかかるため、
  その間に capstone で直接逆アセンブルしたらそれだけで全部読めた。Ghidra の解析は途中で止めた
  (Switch ローダー拡張は結局使っていない)。
- `0x724550` の処理は 3DS 版と同じ形:
  UID(7B) → HMAC(`DKtjn3JAZc`) → ニブル置換 → HMAC(`5g9D63n8Mt`) → `h2[28:32]` を
  `rev` してワードとして保持、PACK 期待値 `0xEFBE` (= バイト列 `BE EF`)、読み出し範囲 0x1C〜0x2A。
  - ニブル置換はコンパイラが `(key, value)` ペア表 (`0x1257c20`) を引く形に展開していて、
    インデックス計算 (`0x1299990` の表と csel) は結局「ニブルそのもの」になる。
- `0x9597d8` の PWD_AUTH 組み立ては `1B, w>>24, w>>16, w>>8, w` なので、送信順は `h2[28], h2[29], h2[30], h2[31]`。
  応答 2 バイトを PACK と比較。
- `0x724850` は保護データ 48 バイトを AES-128-CTR で復号し、3 ブロックそれぞれのチェックサムを確認。
  鍵・IV の作り方も 3DS 版と同じ。
- 手持ち 2 枚のパスワード: `04 70 BC C2 DB 64 81` → `CC 0F 5D 9D`、`04 50 6F 22 D4 64 80` → `4F F5 9D 34`。

## 2026-09-24 実機検証 (00.nfc)

- Flipper Zero で UID `04 70 BC C2 DB 64 81` を PWD `CC 0F 5D 9D` でアンロック → PACK `BE EF` 確認。
- 解除後ダンプ `Data/00_Unlock.nfc` (45 ページ全読み)。ページ 43/44 には Flipper が PWD/PACK を埋めている。
  CFG: ページ 41 = `04 00 00 1C` (AUTH0=0x1C)、ページ 42 = `C0 00 00 00`。
- ページ 28〜39 を `medal_pwd.py --decrypt` で復号 → `30 33 31 35 4D 43 4E 00..00 A7` + 32 バイトの 0。
  3 ブロックともチェックサム一致。鍵・IV の導出も含めて算出式が正しいことを確認。
- 平文 `"0315MCN"` の `MCN` は URL トークン `MCNhnxgjFy...` の先頭 3 文字と同じ。`0315` はメダル ID か (未確認)。
- メダルのシールに `A315` の表記がある → `0315` は妖怪辞典の妖怪番号と推測。
  シールの反対側には `2493225` という数字もあるが不明 (16 進 0x260B29。UID・平文・URL トークンのどれにも見当たらない)。
- 2 枚目のメダルはこの時点では未検証。

## 2026-09-24 復号後の処理 (0x724ae0) の下見

- 平文の 4〜6 バイト目 (`MCN`) を `"%c%c%c"` で文字列にして `strtoul(..., 36)` (36 進数) で数値化
  (`MCN` → 28967)。0 なら エラー 0x1612。
- その数値を 0x48 バイト/エントリの `[min, max]` 範囲表 (実行時データ) で検索し、該当なしならエラー 0x1615。
- 該当エントリの +0x44 フラグが立っていると、UID (8 バイト) をセーブ内の最大 300 件の使用済み UID リスト
  (`aUsedNfcId` と思われる) と照合する → **同じ UID のメダルは 1 回しか使えない種類がある**。
- 平文 16 バイト目 (ブロック 2 の先頭) が 0 でないかどうかをフラグとして保存 (0x3ec)。
- 先頭 4 バイト `0315` はこの関数では使っていない (別の場所で使っているかは未確認)。
- ゲームが送る生コマンドは PWD_AUTH (0x1B)、FAST_READ (0x3A)、WRITE (0xA2) の 3 種類だけ。
  READ_SIG (0x3C, NXP 署名) や GET_VERSION (0x60) は送っていない → 署名チェックは無い。

## 2026-09-24 UID 差し替え .nfc の作成と URL 整合性の下見

- `work/make_nfc.py` を作成。元ダンプを元 UID で復号し、新 UID の鍵で暗号化し直して、UID / BCC0 / BCC1 /
  PWD (ページ 43) / PACK (ページ 44) を書き換える。元 UID を指定すると元ファイルと完全一致する (往復テスト済み)。
- `Data/00_UIDswap.nfc`: UID `04 46 61 81 FA D9 3F`、PWD `60 64 3B 03`、平文は 00 と同じ `0315MCN`。
  NDEF (URL) と Signature は元のまま。実機検証待ち。
- メダル読み取りのステートマシン (関数表 `0x14c33c8`):
  `0x724340` → `0x7243f0` → `0x7244a0` (ページ 0x00〜0x1B を FAST_READ、PWD なし) →
  `0x724550` (鍵を作って PWD_AUTH → ページ 0x1C〜0x2A を FAST_READ) → `0x724850` (復号 + チェックサム) →
  `0x724a50` (終了) → `0x724ae0` (種別判定・使用済み UID 照合) → `0x724e30` ...
- 読み出したページはマネージャ (`*(0x56b7f70)`) の +0x14 + ページ×4 に入る。ページ 28 以降 (+0x84) は復号処理が、
  UID (+0x100) は種別判定が参照するが、**ページ 0〜27 (NDEF/URL) の領域を読む処理はメダル関連の関数には見当たらない**。
  main に `ywg` や URL 解析らしき文字列も無い (`NDEF` のヒットは `INDEF_...` という無関係な語)。
  → ゲームは URL トークンを見ていない可能性が高い (関数ポインタ経由の間接参照までは全部追っていないので断定はしない)。
- メダル処理の中に WRITE (0xA2) 要求を組み立てるコードは無い (書き込み範囲のフィールドは初期化で 0 にされるだけ)。
  WRITE はパススルー処理の汎用機能として存在するだけで、メダル読み取りでは使われていないとみられる。

## 2026-09-24 RomFS からメダル種別表 (MCN の範囲表) を特定

- Linux マシンで base XCI の secure パーティションを展開 → base Program NCA と v2.2.0 update NCA を
  `--basenca` で重ねて `--romfsdir` (8.5GB、約 1 分)。中身はほぼ CRI CPK。
- 解析用 PC には `gamedata*.cpk` (約 23MB) と `text_jp.cpk` (17MB) だけ持ってきた。
  CPK は暗号化されていない @UTF + CRILAYLA → `work/cpk.py` を作って展開。
  中の設定は Level-5 cfg.bin (エントリ = crc32(名前) + 型付き値、末尾にキー名表) → `work/cfgbin.py`。
- 範囲表の正体は `data/common/gamedata/nfc/nfc_lottery_config.cfg.bin` の `NFC_INFO` (506 件)。
  18 個の int = 0x48 バイトで、ゲーム側の表のエントリサイズと一致する。
  - [0] min / [1] max: 36 進 ID の数値。**506 件すべて min == max** で、範囲ではなく実質「メダル種別 ID」の一覧
    (`M3I`=28638 〜 `MQF`)。
  - [2]: 抽選テーブル ID (`NFC_LOTTERY_INFO_TABLE`、中身はアイテムのハッシュと重み)。種別判定を通ると
    0x3e4 に保存され、`StartReadingNFC` スクリプトに渡る。
  - [4]: キャラの chara_base ハッシュ (0 のものが 427 件、妖怪付きは 79 件)。
  - [17] (+0x44): 使用済みリストの選択。0 = 1000 件の永久リスト (一致でエラー 0x1613)、
    0 以外 = 300 件のリスト (一致でエラー 0x1614、`aUsedNfcIdOneDay` と思われる)。
    値は 0/1/2/3/4/5/6/10 があり、0 以外の意味 (日数や回数?) は未確認。
- **`MCN` = 28967 → はむはむ (flag 0、抽選テーブル 0x72165e6e)**。flag 0 なので同じ UID は永久に 1 回しか使えない。
- dictionary_config (RDBN 形式、未解析) で、はむはむのレコードの連番は 126。シールの `A315` / 平文の `0315` とは合わない。
  `0315` の意味は未解明 (ゲームは種別判定に使っていない)。
- 00 のメダルの絵柄ははむはむで正しい → `MCN` = はむはむの対応は確定。
  `0315` と図鑑連番 126 が合わないのは、内部 ID と図鑑番号 (UI) が別管理なためと考えられる
  (1〜3 と 4 で妖怪のラインナップが大きく変わったので、シリーズ共通の番号と 4 の図鑑は単純には一致しない可能性)。
- `work/medal_table.py` で全種別の一覧 (`work/out/medal_table.txt`、Git 管理外) を作れる。

## 2026-09-24 01 のメダル (ホノボーノ) の検証

- UID `04 50 6F 22 D4 64 80` を PWD `4F F5 9D 34` でアンロック → PACK `BE EF`。ダンプ `Data/01_Unlock.nfc`。
- ページ 28〜39 の復号: `30 33 33 30 4D 44 32 00..00 89` (`"0330MD2"`) + 32 バイトの 0。チェックサム 3 ブロックとも一致。
- `MD2` = 28982 → `NFC_INFO` で ホノボーノ (flag 0、抽選テーブル 0xcba1009f)。絵柄と一致。
  NDEF の URL トークンも `MD2...` で始まる。
- シール: `A330`、反対側 `2493225` (00 と同じ数字)。
- **先頭 4 桁と 36 進 ID の関係: 番号 = 数値(ID) − 28652**。
  00: 28967 − 28652 = 315 (A315)、01: 28982 − 28652 = 330 (A330)。
  先頭 4 桁はメダル商品の通し番号 (シールの A 番号) で、図鑑番号とは別の体系。
  `M3I`〜 (28638〜) は計算上マイナスになるので、先頭部分は別シリーズや特殊メダルかもしれない (未確認)。
- `2493225` は 2 枚とも同じ → メダルごとの番号ではなく、共通の番号 (商品やロット、妖怪アーク関連など)。ダンプ内には無い。

## 2026-09-24 ゲーム実機での検証 (UID 差し替え)

- Flipper で次の 2 つをエミュレートし、妖怪ウォッチ4 で読み込ませた。どちらも正しくアーク (はむはむ) として認識された。
  - `00_Unlock.nfc` (元の UID のまま)
  - `00_UIDswap.nfc` (UID `04 46 61 81 FA D9 3F`、データを再暗号化、NDEF の URL と NXP 署名は元のまま)
- 同じ UID を 2 回目に読ませると「一度しか読み込めません」で弾かれた (flag 0 の永久リスト、エラー 0x1613 の経路)。
- これで確定したこと:
  - NXP の署名は確認されていない (UID を変えたので署名は合っていないが通った)
  - NDEF の URL トークンと UID / 平文の整合性も確認されていない (URL は元のまま)
  - 1 回限りの制限は UID 単位。UID を変えて作り直せば別のアークとして扱われる

## 2026-09-24 別種類のアークの作成

- `make_nfc.py` に `--id <36 進 3 文字>` を追加。平文 = `"%04d" % (int(ID,36) - 28652)` + ID + 0 埋め、チェックサム自動。
  番号が負になる ID (`M3I`〜`M3V` など) は `0000` にする (ゲームは使っていない)。
- `00_Unlock.nfc` をベースに、UID は乱数で 5 種類作成 (`Data/arks/`、Git 管理外)。NDEF の URL は元の `MCN...` のまま:
  | ID | No. | 妖怪 | UID | PWD |
  |---|---|---|---|---|
  | M88 | 0156 | ジバニャン | 04 4A F5 EA EB 04 28 | 55 0B 09 D8 |
  | M8N | 0171 | コマさん | 04 B2 2E 83 F1 CA 99 | 1F 9D 4D 2F |
  | M8C | 0160 | キュウビ | 04 92 54 82 A4 B2 FF | 50 E7 12 04 |
  | MA1 | 0221 | フユニャン | 04 57 0F BB B6 15 F3 | 01 0F 15 10 |
  | M8B | 0159 | オロチ | 04 D6 41 99 96 50 88 | 41 B7 4D EB |
  いずれも flag 0 (UID ごとに 1 回限り)、抽選テーブル 0x72165e6e。
- **ゲーム実機で 5 枚とも正しい妖怪として読み込まれた** (M88 もジバニャン)。
  → 種類を決めているのは平文の 36 進 ID だけ。NDEF の URL (`MCN...` のまま) や先頭 4 桁は関係ない。
  同じ妖怪に複数の ID がある場合 (ジバニャン: M3I / M88 / M8M) の違いは未確認。

## 2026-09-24 全 506 種別の報酬一覧 (`work/medal_list.py`)

- 妖怪なしの 427 件も含め、NFC_INFO の各フィールドを解決してテキスト化 (`Data/medal_list.txt`、Git 管理外)。
- フィールドの解釈 (推測込み):
  - [2] 抽選テーブル ID、[5][8][9] 追加抽選テーブル ID
  - [6] 特別抽選テーブル (カテゴリ 5 にだけある。ゴッド魂・装備・魔神のアークなど、メダル固有の限定報酬。29 件)
  - [10] アイテム (装備品・大事なもの。item_config の ITEM_EQUIP_INFO / ITEM_IMPORTANT_INFO)
  - [12][16] flag_config のフラグ (番号のみで名前なし)
  - [3][11] 0/1 (不明)
- 抽選テーブルは `NFC_LOTTERY_INFO` 1〜7 の 7 カテゴリに分かれていて、同じテーブル ID がカテゴリごとに別の中身を持つ:
  1/6 = けいけんちだま・こけし・漢方、2 = 封魔のアーク、3 = ガシャコイン・星コイン、4 = 値だけ (0/1/2、回数か個数?)、
  5 = 限定アイテム、7 = 食べ物 (値が個数らしい)。
  1 回の読み込みでどのカテゴリを引くかはゲーム本体 (`funcLuaMenuNfcCommand`) 側で、未解析。
  `nfc_menu_4.00.11.lua.bin` は表示用 (`DisplayLimitedItems`, `DisplayCountableArk` など) だけ。
- アイテム名は item_config の 3 番目の値 (名前ハッシュ) → `text/ja/item_text.cfg.bin` の NOUN_INFO。
- `NFC_LOTTERY_COUNT_INFO` (8 件)、`NFC_QUANTITY_INFO` (26 件) の意味は未解析。

## 2026-09-24 限定報酬付き種別の実機テスト (MBU / MC1) と調査

- 実機結果: `MBU` (魔神のアーク・不動明王) は「対応していないアークです。最新バージョンに更新して…」で拒否。
  `MC1` (魔神のアーク・明王【界】) は読み込めたが、魔神のアークは出ず普通のアイテム抽選だった。
- エラー表示は `script.cpk` の `lua/menu/nfc_menu_4.00.11.lua.bin` の `DispErrorMessage`。Lua 5.2 バイトコードを
  自作の `work/luadis.py` で逆アセンブル。`_errorID` → system_text:
  19 = 一度しか読み取れません、20 = 1 日 1 回まで、21 = 対応していないアーク、ほか 4/5/6/11/12/14/15。
  ゲーム側のエラーは `[obj+0x3f0]` に halfword で `0x16xx` (上位 0x16 = エラー状態、下位 = errorID) を書く。
  0x1611 = 復号チェックサム NG、0x1612 = ID が 0、0x1613 = 永久リストに UID あり、0x1614 = 1 日リストに UID あり、
  0x1615 = ID が NFC_INFO に無い (`0x724bb4` 種別判定 と `0x724e30` 抽選の最初の再検索の 2 か所だけ)。
- `MBU` は v2.2.0 の NFC_INFO にも base v1.0 (v0, 218 件) の NFC_INFO にもある。NFC_INFO のローダ (`0x4748f0`) は
  全行をそのままコピーしていて選別しない。→ コード上は MBU で 0x1615 になる経路が見当たらない。
  作ったファイルの ID が実際には MBU でなかった可能性がある (そのファイルは削除済みで確認不能)。
  → MBU で作り直して読ませたら正常に読み込めた (普通のアイテム抽選)。一時的な問題か入力ミスとみて保留。再発したら再調査。
- v1.0 (base) の NFC_INFO は 218 件、v2.2.0 は 506 件 (288 件追加、削除なし)。MCN/MD2/MC1/MA1 は追加分。
- 抽選処理 (`0x724e30`〜`0x7266xx`) の下見:
  - 抽選カテゴリ (`NFC_LOTTERY_INFO`) は `0x4d9cf0(表, カテゴリ ID)` で ID を二分探索して取る。
    呼び出しは ID 2 / 3 / 4 / 7 などを直値で渡している。[6] (特別) の非 0 判定の後は ID 4 で引いている (0x725c54)。
  - 平文ブロック 2 の先頭バイトが非 0 (`0x3ec`) だと、[8] (0 なら [2]) の表をカテゴリ 6 で引く追加抽選がある
    (Lua の `mesh_omake_power` = おまけパワーと思われる)。
  - 特別報酬が出る条件は未解明。system_text に「同じ色のアークを もっとかざせば 何かが起こりそうだ…」
    「通常のアークは一度だけ、特殊アークや妖聖剣は 1 日 1 回 何度も読み込めます」があり、
    魔神のアークはカラーアーク (同色を複数回かざす) の仕組みで出る可能性がある。
  - **特別報酬 / アイテム抽選の条件はこれ以上追わない方針** (2026-09-24)。

### 残っているもの

- 『妖怪ウォッチ4++』(PuraPura) は未調査 (別タイトル、鍵が違う可能性あり)。
